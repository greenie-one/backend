apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "greenie-mvp-backend-dev.fullname" . }}-test-connection"
  labels:
    {{- include "greenie-mvp-backend-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "greenie-mvp-backend-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
