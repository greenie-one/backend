export interface GetLocationResponse {
  id: string;
  longitude: Number;
  latitude:Number;
  user: string;
}

export interface LocationResponse {
  code:string,
  longitude:Number,
  latitude:Number
}
